# 简介

Industryts 是一个高性能的时间序列处理库,专为工业数据处理而设计。它结合了 Rust 的性能和 Python 的易用性,提供了比 pandas 快 10-100 倍的数据处理能力。

## 核心特性

### 🚀 极致性能
- 基于 Rust 构建,利用 Polars 的列式存储
- 零拷贝数据传输
- 并行处理能力
- 在典型时间序列操作中比 pandas 快 10-100 倍

### 🐍 Python 友好
- Pythonic API 设计
- 完整的类型提示
- 与 pandas 类似的编程模式
- 无缝集成 Python 数据科学生态

### 📊 全面的操作
- 数据清洗和预处理
- 时间序列重采样
- 特征工程
- 数据变换和聚合
- 所有操作都针对工业数据优化

### ⚙️ 双重 API 设计
- **编程式 API**: 灵活的 Python API,适合交互式开发
- **声明式 API**: 基于 TOML 的配置,适合生产环境和可重现工作流

## 架构设计

```
┌─────────────────────────────────────────┐
│         Python API Layer                │
│  (industryts Python Package)            │
├─────────────────────────────────────────┤
│         PyO3 Bindings                   │
│  (Python ↔ Rust 通信)                   │
├─────────────────────────────────────────┤
│      Rust Core Engine                   │
│  (industryts-core)                      │
├─────────────────────────────────────────┤
│   Polars DataFrame Engine               │
│  (列式存储 + 并行处理)                   │
└─────────────────────────────────────────┘
```

## 主要组件

### TimeSeriesData
时间序列数据的核心容器,基于 Polars DataFrame。

```python
import industryts as its

# 从 CSV 加载
data = its.TimeSeriesData.from_csv("sensor_data.csv")

# 从 Polars DataFrame 创建
import polars as pl
df = pl.read_csv("data.csv")
data = its.TimeSeriesData(df)
```

### Pipeline
数据处理流程,支持链式调用和 TOML 配置。

```python
# 编程式 API
pipeline = its.Pipeline()
pipeline.fill_null(method="forward")
pipeline.lag(columns=["temperature"], periods=[1, 2, 3])
pipeline.standardize(columns=["temperature", "pressure"])

# 处理数据
result = pipeline.process(data)
```

### Operations
各种数据处理操作,包括:
- 数据清洗: `fill_null`, `drop_null`, `clip`
- 时间操作: `resample`, `lag`, `rolling`
- 特征工程: `standardize`, `normalize`, `one_hot_encode`
- 聚合: `groupby`, `aggregate`

## 使用场景

### 传感器数据处理
- 实时数据清洗和预处理
- 特征提取和工程
- 异常检测

### 工业监控
- 设备性能分析
- 预测性维护
- 能耗优化

### 金融时间序列
- 股票数据分析
- 技术指标计算
- 风险评估

### 物联网数据
- 大规模数据处理
- 实时流处理
- 多源数据融合

## 性能对比

在 100 万行 × 10 列数据集上的测试:

| 操作 | pandas | industryts | 加速比 |
|------|--------|------------|-------|
| 重采样 | 2.3s | 0.05s | 46x |
| 滚动平均 | 1.8s | 0.03s | 60x |
| 滞后特征 | 1.2s | 0.02s | 60x |
| 完整流程 | 12.5s | 0.15s | 83x |

## 下一步

- [安装指南](/guide/installation) - 详细的安装说明
- [快速开始](/guide/quick-start) - 创建第一个处理流程
- [核心概念](/guide/concepts/timeseries) - 深入理解 TimeSeriesData
